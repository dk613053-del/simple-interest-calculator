# Simple Interest Calculator

A simple tool to calculate interest based on principal, rate, and time.

## Formula
The formula used to calculate simple interest is:
SI = (P * R * T) / 100

Where:
- P = Principal amount
- R = Annual interest rate
- T = Time (in years)

## Usage
Input the values for Principal, Rate, and Time to get the total interest earned or owed.
